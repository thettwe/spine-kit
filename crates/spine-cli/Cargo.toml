[package]
name = "spine-cli"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "spine"
path = "src/main.rs"

[dependencies]
spine-canon = { path = "../spine-canon" }
spine-manifest = { path = "../spine-manifest" }
spine-init = { path = "../spine-init" }
spine-template = { path = "../spine-template" }

[lints]
workspace = true
