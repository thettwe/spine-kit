# Adversarial verification findings — 2026-08-27 workflow

Raw output of the four verifier agents, 2026-08-27.

**Status: worked down 2026-08-28.** Every `every-landing` and `serious` finding
is fixed, and the minors that were correctness rather than tidiness with them.
Each fix carries its citation in the code and a test named for the rule it pins.

Two findings were **not** fixed and became corpus questions instead, in
[`OPEN-questions.md`](OPEN-questions.md): the Dart `''` escape, and the §4.1
Python anchor's disagreement with §3.7 (whose fix moves `closure_digest` and so
needs the matching clause in §4.1).

Three findings were **the verifier's, not the code's**, and are recorded as
divergences rather than defects — the `ids=` u64 bound, the empty-`fn` refusal,
and the `tool=` construction, the last of which is a corpus defect handled
correctly and now filed as question 3.

## spine-collect — verdict: needs-work

### Confirmed findings
- **[serious]** `ResultFile::floor_holds()` returns `true` for a `base-collect-failed` file. RF §7.3 names this exact case as the one it exists to close: "This closes the one place where an empty or near-empty file could pass vacuously: a quick-lane landing has no frozen ids, so its G1 is the `B` floor alone, and a `base-collect-failed` file would otherwise satisfy it by emptiness." `pair_passed` carries the `status` conjunct deliberately (file.rs:236-239: "inside this function rather than left to the caller because RF §7.3 makes forgetting it the one way an empty file passes vacuously"), but `floor_holds` is `self.base.iter().all(...)` and `all` over an empty floor is vacuously true — the guard never runs. Reproduced: a two-line file with `ids=0` and `end.status=base-collect-failed` gives `floor_holds()==true`, `credits_outcomes()==false`. The crate's own test PINS the fail-open (`assert!(read.floor_holds(), "vacuous over an empty floor")`). A gate layer that reads `floor_holds()` for clause 2 without separately evaluating clause 0 credits a file with no floor at all. Fix: `if !self.status.credits_outcomes() { return false; }` at the head of `floor_holds`, exactly as `pair_passed` and `frozen_entry_passed` already do.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/src/file.rs:285 (and the pinning assertion at src/file.rs:748); RF §7.3, "Two rules make this safe"_
- **[serious]** `collect()` over an empty adapter slice writes a fully green, zero-evidence file. Reproduced: `status=complete`, `ids=0`, no records, `floor_holds()==true`, `exit_is_zero()==true`. `fold()` returns `Complete` on an empty contribution list (`.min().unwrap_or(Status::Complete)`), and `collect` never checks that the invocation set is non-empty. The crate's doc comment argues it is "unreachable in a conforming repository — MF §3.11's `langs-empty` keeps an empty `params.langs` off trunk" — I confirmed `Manifest::parse` does refuse `"langs":[]` with `langs-empty` — but that check is in another crate and `collect` does not consult it, does not take the invocation set, and does not compare its adapter slice against `invocation_set()`. Where the corpus is silent the fail-closed reading was available (refuse at step 3, or fold a runner-less run to something other than `complete`); the code took the fail-open one, and combined with the `floor_holds` vacuity above this is precisely the vacuous quick-lane pass RF §7.3 spends a paragraph closing.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/src/collector.rs:615-622 (`fold`) and :440 (`collect`); RF §7.3 fold, RF §6.2_
- **[serious]** The write path performs no `(runner, id)` uniqueness check, so `collect()` can emit a file this crate's own `ResultFile::parse` rejects as `result-malformed`. Reproduced: two adapters whose `token()` returns `pytest` produce `ids=2` with two identical `base` lines and two identical `result` lines; re-reading those bytes yields `DuplicatePair { section: Base, runner: "pytest", id: "a.py::x" }`. `ResultFile::new` sorts both sections but never verifies strict ascension, and `collect` takes `adapters: &mut [Box<dyn RunnerAdapter>]` without ever consulting `invocation_set()`, which is the only place the dedup the builder reasoned about actually lives. RF §11 item 5 makes pair uniqueness a conformance obligation on the writer as well as the reader; RF §4.4 makes the repeat malformed. The one-line fix is for `check_order`'s relation to be reused on the write side (or for `ResultFile::new` to return a `Result`).
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/src/file.rs:52-67 (`ResultFile::new`) and src/collector.rs:245-258 vs :440-446; RF §11 item 5, RF §4.4_
- **[minor]** No test anywhere composes the write side with the read side: `grep -c 'ResultFile::parse' tests/collector_order.rs` is 0. All ten integration tests inspect the in-memory `ResultFile` struct or count lines of `to_bytes()`; none re-reads the bytes `collect` produced. That is the missing probe that hides the duplicate-pair defect above, and it is the cheapest assertion in the crate — `assert!(ResultFile::parse(&file.to_bytes(), TREE, format).is_ok())` at the end of every scripted-runner test.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/tests/collector_order.rs (whole file)_
- **[minor]** A test that cannot fail, guarding a refusal path with no coverage. `a_zero_timeout_refuses_the_job_rather_than_disabling_the_deadline` claims in its own doc comment that "The refusal is checked twice on this path", but its second half builds a `Policy` struct literal with `timeout_secs: 0`, asserts that field equals 0, and asserts `Refusal::TimeoutOutOfRange.token() == "timeout-out-of-range"` — a `&'static str` compared to itself. Neither statement calls `Policy::read` or the private `timeout()` whose `_ => Err(Refusal::TimeoutOutOfRange)` branch is the thing under test. I probed every out-of-range shape through the public API — `0`, `-1`, `"1800"`, `18.5`, `99999999999999999999` — and `Manifest::parse` refuses all five first (`timeout-out-of-range` / `manifest-not-json`), so the collector's own step-1 deadline refusal is executed by nothing in the test suite and is unreachable through the dependency's public constructor. Defensible as defence in depth; the defect is the test asserting coverage it does not have.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/tests/policy_step_one.rs:70-100 vs src/collector.rs:206-216_
- **[minor]** `ResultFile::new` accepts `Status::BaseCollectFailed` beside a non-empty body and silently writes `ids>0` against that status, which RF §7.3 forbids in terms ("`ids=0`, and **no `base` and no `result` records are written at all**, from any runner"). The suppression lives only in `collect` (collector.rs:572-575), so the invariant is a property of one call site rather than of the type. The crate makes exactly the opposite argument for `ids=` itself — it is derived inside `new` "because a value the caller supplies is a value the caller can get wrong" — and the same reasoning applies here.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/src/file.rs:52-67; RF §7.3, "Collection on `B` is all-or-nothing across runners"_
- **[minor]** `BaseOutcome::exempts_from_findings()` is named and documented as deciding RF §8.5 clause 2's carve-out, but the carve-out has a second conjunct this crate exposes no way to compute. RF §8.5: "**It does not reach the *went away* shape, and that is the boundary.** … a vanished `xfail` or `skipped` id is allocated below exactly as any other vanished id is, and still takes a `class=protected` `G8:<path>` review." The doc comment quotes RF §4.4's "exactly one consumer" and never mentions the shape restriction, and the crate offers no `went_away`/`did_not_pass` discriminator beside `pair_passed`. A gate layer using this predicate alone exempts a deleted `xfail` test from the G8 review that is the whole point of the boundary — the fail-open direction. Renaming it (`was_xfail_or_skipped_on_b`) or documenting the second conjunct would close it.
  - _/Users/thettwe/Works/spine-kit/crates/spine-collect/src/outcome.rs:138-153; RF §8.5 clause 2, third bullet_

### Spec divergences
- `Header::parse` refuses an `ids=` value that is inside RF §4.2's grammar but above `u64::MAX`, reporting `HeaderValueOutOfGrammar { key: "ids", why: "does not fit in u64" }`. RF §4.2 fixes only "non-negative decimal, no sign, no leading zero except `0` itself" and states no upper bound. Fail-closed in effect (such a count can never equal the `base` record count, so §4.2's agreement rule would reject it anyway, and both collapse to the same `result-malformed` token), and the builder disclosed it. header.rs:369.
- `parse_spine_test_payload` returns `None` for a payload whose function id is empty. RF §6.5 names exactly two malformed shapes — "A payload with no `U+0020`, or whose first token is outside §4.4's grammar" — and does not list this third. Consistent with §4.4's `fn` "Non-empty" and fail-closed, but an added refusal not in the spec's enumeration. collector.rs:649-651.
- `Policy::expected_tool_token` strips a `sha256:` prefix from `cli.dist_hash` before re-adding the separator, diverging from RF §8.3 step 2's literal construction recipe ("`<cli.version>` `+sha256:` `<cli.dist_hash>`"). This is a genuine corpus defect handled correctly, not a crate defect: MF §3 stores `dist_hash` as `sha256:<hex>` (MF §8.3's published manifest carries `"dist_hash":"sha256:6f49644f…744db"`), so the recipe read literally yields `1.4.0+sha256:sha256:6f49…`, which is outside RF §4.2's grammar and is not RF §10's published `tool=`. The crate documents the divergence at collector.rs:181-189 and pins the vector as arbiter. This should be raised against `result-file.md` §8.3 step 2.
- The five `Refusal` tokens (`tool-bytes-mismatch`, `lang-without-adapter`, `isolation-unsupported`, `timeout-out-of-range`, `merge-conflict`) are the implementation's; the corpus fixes the behaviour ("fail the job, write nothing") and no token. Two reuse MF §3.11's spellings. Disclosed as DERIVED and harmless — a refused run writes no file, no report and no seal, so none of these bytes can reach a wire or a signature.
- `Header::tool_parts` returns the second half as `sha256:<hex>` rather than the bare `<hex>` RF §4.2's "splits at its **last** occurrence of the literal `+sha256:`" would yield. Documented in the doc comment and convenient (it is the form MF stores), but a reader taking the spec literally gets a different second element.

## spine-graph — verdict: needs-work

### Confirmed findings
- **[serious]** `serialize` never validates an edge's `from`/`to` at all — no `check_node_id`, no repo-prefix test, no ASCII test. DM §5.3 says a malformed endpoint id "surfaces as `id-out-of-grammar`" and DM §17 items 4 and 10 require every string to be ASCII-after-`esc` and every id to match its kind's row. PROVED: a graph holding one `modifies` edge with `from = "myrepo/cs:\u{e9}bad id"` and `to = "NOT-EVEN-REPO-PREFIXED"` serializes to `Ok`, and the produced dump contains raw bytes above 0x7E — the crate's own central invariant escapes through the edge section. `check_node_id` is already `pub` and is called two loops earlier for nodes.
  - _crates/spine-graph/src/dump.rs:189-206 (edge loop) vs. :172 (node loop calls check_node_id); docs/spec/dump.md §2.3 Strings row, §5.3 "No dangling edges", §17 items 4 and 10_
- **[serious]** Oid-valued attrs (`blob`, `base`, `head`, `tree`, `landing`, `valid_from`, `valid_to`, `freezes.oid`, `voided_by`) are typed `Str` and never checked against `is_oid`. PROVED: an intent node carrying `blob: "abc123f"` and `landing: "ABC"` serializes to `Ok`. Worse, the *conformance checker* written to catch this is inverted: it only asserts `is_oid` when `s.len() == format.hex_len()`, so an abbreviated oid — the single case DM §5.2 names ("an abbreviation's length is a function of the repository's object count") — is skipped rather than caught. This is a live G10 hazard, not a cosmetic one: the scratch clone `S` and the clean clone of `S` hold different object counts by construction (DM §4.1), so an abbreviated oid can differ in length between the two sides and produce a genuine `reconstruction-failed`.
  - _crates/spine-graph/tests/dm_17_conformance.rs:102-107 (`&& s.len() == format.hex_len()`); no oid check anywhere in crates/spine-graph/src/dump.rs; docs/spec/dump.md §10 rule 9, §17 item 5, §5.2_
- **[serious]** DM §7.2's "always" presence column is enforced nowhere, and that defeats a check the crate did write. The member-changeset rule is guarded by `if let Some(AttrValue::Bool(false)) = attrs.get("landing") && attrs.iter().count() != 1` — so a changeset that omits `landing` entirely escapes it. PROVED: a changeset node with `{"lane":"gated","mode":"team"}` and no `landing` serializes to `Ok`. Likewise an `intent` node with `attrs: {}` (no `status`, `title`, `template`, `blob`, `landing`, `base`, `reopen_count`, `late_reopen_count`) serializes to `Ok`, and `check_enum` returns `Ok` for every absent enum attr. A `signer` with `roles: []` and an `approval` carrying `class` on a `signoff` event are likewise accepted.
  - _crates/spine-graph/src/dump.rs:314-323 (`check_node_attr_domains`, Changeset arm) and :404-412 (`check_enum` matches `_ => Ok(())` on absence); docs/spec/dump.md §7.2 presence column, §7.3_
- **[serious]** `Graph::add_node` keys the §5.5 collapse on `id` alone, so two nodes of *different kinds* sharing one id silently collapse to one and the other vanishes from the dump. PROVED: adding `intent myrepo/INT-042` and `adr myrepo/INT-042` (legal — DM §5.2 fixes no grammar for an `adr` local id, and the crate accepts any non-empty string) yields a one-node graph; the adr is dropped with no refusal. Two consequences. (a) When the two records' `canonical(attrs) ‖ NUL ‖ src` tails tie, the surviving record's `kind` is whichever was inserted first — walk order, precisely what DM §13.5 refuses ("'the first one the walk happened to reach' is walk order, which is not a specification") and what DM §10 rule 5 forbids. (b) `serialize`'s duplicate-id guard is therefore unreachable dead code: `Graph`'s fields are private and `add_node` is the only insertion path, so `seen_ids.contains(...)` can never fire — and it has no test.
  - _crates/spine-graph/src/store.rs:124-131 (`add_node`, `.find(|n| n.id == node.id)` then `if node_tail(&node) < node_tail(existing)`); crates/spine-graph/src/dump.rs:176-179 (unreachable guard); docs/spec/dump.md §5.5, §13.5, §10 rule 5_
- **[minor]** `Src::Trailer`'s `<Name>` is interpolated into the rendered citation raw — no `esc`, no ASCII test; `Src::check` only asserts it is non-empty. DM §2.4 says `esc` applies to "every `src`". PROVED: a node whose `src` is `Src::Trailer { name: "Spine-Café" }` serializes to `Ok` and puts non-ASCII bytes into the artifact. Latent, since real trailer names are ASCII, but it is a second hole in the same invariant as finding 1, in the one function whose whole job is to close it.
  - _crates/spine-graph/src/schema.rs:786 (`Src::Trailer { sha, name } => sha_ok(sha) && !name.is_empty()`), :752 (`render`); docs/spec/dump.md §2.4_
- **[minor]** `EdgeKind::endpoints()` — the table DM §13.4 calls load-bearing ("Getting one backwards is a whole-dump diff, so all fifteen are written down") — is never consulted by `serialize` or by the conformance checker. Its only caller in the whole workspace is one unit test asserting one of its fifteen rows. PROVED: a `verified_by` edge running ac → test (the reverse of what the table states) serializes to `Ok`. The crate knows the direction and declines to enforce it.
  - _crates/spine-graph/src/schema.rs:222-243 (`endpoints`); sole caller crates/spine-graph/src/schema.rs:872; docs/spec/dump.md §5.3 "Direction, where PB leaves it implicit", §13.4_
- **[minor]** The conformance file's own bookkeeping overstates what it checks. Its header says "Twenty of the twenty-four are checkable here" and then names items 20, 21, 22, 23 and 24 as skipped — 24 − 5 = 19, not 20 (and item 3's member-sorting half is explicitly not asserted, so 18 in substance). The comment "Items 7, 8 and 10: every record's shape, and every id in its kind's row" labels a loop whose body performs only the repo-prefix half of item 10: `check_node_id` is never called, item 7 (no unknown member name anywhere) is never checked, and item 8 is never checked.
  - _crates/spine-graph/tests/dm_17_conformance.rs:4-6 and :111-115; docs/spec/dump.md §17 items 7, 8, 10_
- **[minor]** Item 13's assertion cannot fail. It calls `g.ordered_nodes()` / `g.ordered_edges()` — which sort by `node_key` / `edge_key` — then asserts the resulting key sequence is ascending. Any correct sort passes; the only thing it really tests is key uniqueness (item 14). It never touches the dump's bytes and never checks item 13's first clause ("the node section precedes the edge section"). The real evidence for item 13 lives entirely in the byte-for-byte vector test in the other file.
  - _crates/spine-graph/tests/dm_17_conformance.rs:139-143; docs/spec/dump.md §17 item 13 ("Verified by re-sorting the records by key and comparing")_
- **[minor]** `Header.trunk` is accepted empty. DM §3.1 makes `trunk` "the resolved trunk **branch name**" and DM §4.2 makes resolution reach `not-installed` when there is none. PROVED: `trunk: b""` serializes to a 101-byte header carrying `"trunk":""` and exits the happy path.
  - _crates/spine-graph/src/dump.rs:146-160 (`serialize` header validation checks `repo`, `head`, `trust_root` and not `trunk`); docs/spec/dump.md §3.1, §4.2_

### Spec divergences
- DM §17 item 10 is enforced for node ids only; edge `from`/`to` are exempt from the grammar, the repo prefix and the ASCII profile (finding 1).
- DM §10 rule 9 / §17 item 5 ("lowercase hex at object_format's full length, never abbreviated, never uppercase") is enforced only for node ids and the two header oids, never for the nine oid-valued attrs (finding 2).
- DM §7.2's presence column ("always") is not enforced for any attr of any kind; the member-changeset rule is bypassable by omitting `landing` (finding 3).
- DM §5.5's node collapse is keyed on `id` alone rather than on the element, so two different-kind nodes with one id merge silently and, on a tail tie, walk-order-dependently — the exact residual DM §13.5 refuses (finding 4).
- DM §2.4 ("`esc` applies to … every `src`") is not applied to `Src::Trailer`'s `<Name>` (finding 5).
- DM §5.3's direction paragraph / §13.4 is modelled in `EdgeKind::endpoints()` but never enforced by the serializer (finding 6).
- DERIVED, and correctly flagged as such by the crate: `Src::FileLine` and `Src::ShippedFloor` refuse as `provenance-invalid`, a token DM §4.4 defines for citations *outside* PB §6.1's grammar while both productions are inside it. Fail-closed, so acceptable, but no published token covers it.
- DERIVED: a `repo` outside `^[A-Za-z0-9._-]+$` (or over MF §3.1's 64-byte bound) refuses with DM §5.2's `id-out-of-grammar` rather than MF §3.1's `repo-out-of-grammar`, while applying MF's length bound that DM §5.2 does not state. Defensible per DM §16's handover note, but it is a token/grammar mix from two documents.
- DERIVED: `adr` local ids are checked only for non-emptiness (DM §5.2 fixes no grammar), which is what makes finding 4's cross-kind id collision reachable from a conforming repository.
- DERIVED and sound: `tool_version_from_seal` splits at the last `+sha256:` rather than the first `+`, per MF §3.2's `[0-9A-Za-z._+-]+` `cli.version` grammar and CI §5.5's spelling; a `tool=` with no `+sha256:` is returned whole.
- DERIVED and sound: `supersedes` runs superseding → superseded, `superseded_by` runs superseded → superseding, and `Graph::add_supersession` emits both. DM §5.3 names neither; PB §6.6 gives only "the indexer emits `superseded_by`".
- DERIVED and sound: `g10_compare` carries `dump_version`/`schema_version` beside the bytes rather than parsing line 1, honouring DM §1's "nothing in spine ever reads a dump" while satisfying DM §3.2's refuse-rather-than-compare rule.
- Not a divergence, but worth recording: `Src::check`'s `line >= 1` correctly reduces DM §5.4's "decimal integers ≥ 1 with no leading zero", since a `u64` cannot spell a leading zero.
- Not implemented and correctly reported as such: the SQLite store and `PRAGMA user_version = 7` persistence, the whole PB §6.2 derivation, DM §4.2 trunk resolution and the `not-installed` exit-2 path as runtime behaviour, stdout/stderr, and G10 steps 1–4.

## spine-isolate — verdict: unsound

### Fabricated values
- `lo flags=0x9` in `netlink::build::the_measured_fresh_namespace` (and the corpus source it copies, .build-notes/FINDINGS-isolation.md:111) is not a value real netlink ifi_flags can hold for the stated condition. In a namespace "made by nothing but `unshare(CLONE_NEWNET)`" loopback is DOWN, so ifi_flags is 0x8 (IFF_LOOPBACK alone); once brought up the kernel also sets IFF_RUNNING (0x40) and IFF_LOWER_UP (0x10000), giving 0x10049 — iproute2 hides IFF_RUNNING when printing, which is likely where 0x9 came from. Either way 0x9 is asserted, not measured. Behavioural impact is nil (only IFF_UP and IFF_LOOPBACK are read), but the test `the_measured_fresh_namespace_is_ten_devices_nine_of_them_down` asserts `links[0].is_up()` and `filter(is_up).count() == 1` on a premise no kernel produces.
- `overlay dev=97 ino=2` / `hostroot dev=79 ino=34252584` in `probe::tests::a_probe_root_equal_to_the_collectors_root_fails_p3_forever` and `probe::tests::host()` are published numbers used as literal inputs, recomputable from nothing the test holds. `decide_p3` only compares the two pairs for equality, so the test passes for ANY two distinct pairs — the specific measured numbers carry zero verification weight while the test name ("the collector's root", "the overlay's measured pair") implies they do.
- The ten-device list and its flag set are encoded by `build::the_measured_fresh_namespace()` from the same literal table that `netlink::tests::the_measured_fresh_namespace_is_ten_devices_nine_of_them_down` then asserts. The round trip exercises the encoder/parser pair but corroborates the published measurement with nothing outside the test; the builder's claim that RF §7.1's measurement is "reproduced from bytes" is circular. No cryptographic digest, hash or fingerprint appears anywhere in the crate (grep confirmed), so there is no unreproducible digest to report.

### Confirmed findings
- **[every-landing]** Arrangement 2 (root collector) writes a uid_map that makes the host see the child's files owned by uid 0, so P2's host limb fails on every host, forever. `map_as_root_collector` writes `format!("{child_id} 0 1")` = "100000 0 1". A uid_map line is `<id-inside> <id-outside> <count>`, so this maps inside-100000 to OUTSIDE-0: the child reports uid 100000 (first limb passes) but every file it creates is owned by kuid 0 on the host. RF §7.1 P2: "the created file's owner as *the host* sees it is neither `U` nor 0" — and `decide_p2` correctly fails on `owner.uid == 0`. Under arrangement 2, U is 0 and the mapping must run the other way (inside 100000 -> outside 100000, child then setuid(100000)). Result: profile=none for every container-requesting landing on the ordinary container-based CI runner. No test asserts the map string; the whole Linux half is untested.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/linux.rs:46-51 (`map_as_root_collector`, `uid_map` = "100000 0 1"), decided against /Users/thettwe/Works/spine-kit/crates/spine-isolate/src/probe.rs:470-476 and docs/spec/result-file.md §7.1 P2 row_
- **[every-landing]** Arrangement 1 (delegated range) maps the child to inside-id 0 with a range of width 1, so the probe's reported real and effective uid/gid are 0 — which P2 excludes outright. `install_identity` calls `map_via_helpers(pid, 100000, 1)`, which runs `newuidmap <pid> 0 100000 1`: inside 0, outside 100000. The doc comment states this intent verbatim ("the child sees `0`, the host sees `first_subordinate_id`"). RF §7.1 P2: "The reported ids all lie outside `{0, U, Ug}`" — `decide_p2`'s `excluded = [0, U, Ug]` fails on 0. With count 1 and inside base 0 there is no other id inside the namespace for the child to setuid to, so P2 is unpassable under arrangement 1 on every host, forever — the exact dead-test shape RF §7.1 says the two arrangements exist to avoid.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/linux.rs:60-90 (`map_via_helpers` args `pid 0 first_subordinate_id count`; `install_identity` passes count 1)_
- **[every-landing]** P3's process-table limb can never be enumerated: the mount sequence never mounts procfs inside the boundary, yet `measure_separation` reads `/proc`. After `pivot_root` into an upper-less overlay whose lower layer is `/`, submounts of the job's root are NOT part of the overlay — `/proc` is the bare (empty) mountpoint directory from the root filesystem, and the old root is `umount2(MNT_DETACH)`ed immediately after the pivot. `read_dir("/proc")` therefore yields no numeric entries, `pids` is empty, and `decide_p3`'s own DERIVED guard ("the probe's process table does not contain the probe — nothing was enumerated") fails P3. `mount_sequence` already carries one DERIVED step (`MakeRootPrivate`) for exactly this class of kernel prerequisite; a `mount -t proc proc /proc` step is missing. Consequence: P3 fails on every Linux host, for every configuration, forever — profile=none always.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/linux.rs:208-221 (`measure_separation` reads /proc) vs /Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:148-172 (`mount_sequence` has no procfs step) and linux.rs:154-174 (pivot_root + MNT_DETACH of put_old)_
- **[serious]** `install_identity` hard-codes 100000 as the outside subordinate id and never reads the range the host actually delegated. `prereq::identity_source` only answers *whether* a non-empty `/etc/subuid` line exists (`subordinate_range_exists` parses `name:start:count` and discards `start` entirely), so the discovered range never reaches the mapper. On any host whose delegated range does not begin at 100000 — the common case for the second and later `useradd` accounts (165536, 231072, …) and for any site-configured allocation — `newuidmap` refuses the mapping and the boundary is disposition 2. The seam that would carry the value (`IdentitySource`) is a fieldless enum by design, so there is no path for it.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/linux.rs:78-90 and /Users/thettwe/Works/spine-kit/crates/spine-isolate/src/prereq.rs:288-306 (`subordinate_range_exists` ignores `_start`)_
- **[serious]** The netlink parser accepts a dump with no `NLMSG_DONE` terminator and returns a SHORT LIST — the exact fail-open it claims to prevent. `for_each_message` only errors when a partial header remains (`offset != dump.len()`), so any dump cut on a message boundary parses as a complete, successful enumeration. I verified this by compiling netlink.rs standalone: stripping exactly the 16-byte DONE trailer yields `Ok` with 10 links, and truncating after the first two link messages yields `Ok([lo, tunl0])`. A dump cut after `lo` alone would then PASS P4(a) — a namespace with an inherited, up `eth0` would be licensed as clean. The module's own doc comment says "netlink terminates with NLMSG_DONE and nothing else. Returning the messages read so far would be the short list P4(a) must never see", and the test `a_truncated_dump_is_an_error_and_never_a_short_list` only cuts 6 bytes (mid-header), so it exercises the one case that already works and misses the case that matters.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/netlink.rs:152-188 (`for_each_message`); test at netlink.rs:485-495 does not cover it. Reproduced: `rustc --test` on a copy, `parse_links(&full[..full.len()-16])` -> Ok(10 links)_
- **[serious]** `NetlinkSocket::dump` is the producer that feeds the above: it returns `Ok(out)` whenever `recv` returns 0, or whenever `chunk_ends_the_dump` heuristically says the chunk terminates — it never verifies that the accumulated bytes actually contain `NLMSG_DONE`. Combined with the parser's acceptance of a DONE-less dump, an interrupted or short-read link dump reaches `decide_p4` as `Enumeration::Read(short list)` rather than `Enumeration::Unavailable`, and `decide_p4`'s carefully-derived Unavailable/empty guards are bypassed. This is precisely the hazard the crate names for itself: a probe that measures nothing while reporting a pass.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/sys.rs:266-303 (`dump`, `if got == 0 { break; }` and the `chunk_ends_the_dump` break)_
- **[serious]** Probe step 4's "No probe artifact survives step 6, whatever the outcome" is implemented for the canary only. If P1 limb (c) succeeds, `attempt_containment` has written `b"forged\n"` to the absolute result-file path (`.spine/cache/results/<T>.jsonl`) and nothing anywhere in the crate removes it — `Canary::remove` removes the canary and there is no other unlink in the crate. RF §8.1 makes the untrusted job's artifact exactly one file at exactly one path, and §3 forbids anything that remembers an earlier attempt; a run that aborts between step 6 and step 10 would upload the probe's forgery as the result file. Limb (d) succeeding likewise destroys the result directory and nothing recreates it.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/probe.rs:390-404 (`attempt_containment` writes and removes) vs probe.rs:256-267 (`Canary::remove` is the only cleanup)_
- **[serious]** The restore-phase deadline can be bypassed entirely: `run_restore` does a blocking `stdin.write_all(bytes)` of the whole script BEFORE entering the poll loop that enforces the deadline. A `.spine/restore.sh` larger than the pipe buffer (64 KiB) whose head blocks — `sleep 3600`, a `curl` on a dead socket — leaves `write_all` blocked with `started`/`try_wait` never reached, so the collector wedges forever. RF §7.1 names this exact case as the reason the deadline covers the restore phases ("a restore that waits forever on a socket, is the case that would otherwise wedge a job forever"), and §11 item 10 makes enforcing no deadline non-conformance. No test covers a script that does not drain stdin.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:374-379 (write_all before the loop at m1.rs:381-397)_
- **[serious]** `apply` creates mount points beneath the read-only overlay after it is mounted, which returns EROFS for any path that does not already exist in the job's root. `MountStep::PivotRoot` does `create_dir_all(&put_old)` where put_old lives beneath `new_root` — the upper-less overlay — so it succeeds only if the directory already exists in the lower layer. The crate's only worked `RootSpec` (its test fixture) names `put_old: /run/spine/root/.old`, i.e. a `.old` at the root of the overlay, which exists in no job root: that spec fails the mount sequence with EROFS and yields `CreationFailed` on every host. The same latent constraint applies to `writable_tree_at`, `scratch_at` and the mask path, and it is documented nowhere. The post-pivot `remove_dir(put_old_inside)` is likewise a no-op on a read-only root (swallowed by `let _ =`).
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/linux.rs:154-174 (`create_dir_all(&put_old)` after `OverlayRoot`), fixture at /Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:878-891_
- **[minor]** Prerequisite 4 is vacuously satisfiable: `mapped_id_can_traverse` is `self.traversal.iter().all(...)`, which is `true` for an empty `traversal` vector. `RealHost::new(dir, vec![])` — the exact construction used in the crate's own non-Linux test — makes prerequisite 4 unfalsifiable. A check that cannot fail is the named hazard for this crate, and nothing forces a caller to supply $WORK and $BIN.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/prereq.rs:236-238; constructed with `vec![]` at prereq.rs:471_
- **[minor]** `mount_sequence` emits arrangement (b) (`OutsideTheMountedRoot`) as a plan that guarantees P1 failure, without objection. The overlay's first lower layer is the whole job root, so the real result directory — canary included — is visible read-only in the child's view; P1 limb (a) "read the canary" then SUCCEEDS and P1 fails. The plan also performs no move-into-place after reaping, which §3 requires for that arrangement. The test `the_second_result_directory_arrangement_masks_nothing_and_still_pivots` asserts only that the mask step is absent and a pivot ends the list, so it certifies the broken plan as well-formed.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:166-172 and the test at m1.rs:745-762_
- **[minor]** `decide_p4` never requires loopback to be UP. Prerequisite 5 is "a network namespace … with a loopback device it can bring up", and RF §7.1 notes that otherwise "the runner disposition would be a namespace with no usable interface at all", but the link limb only rejects non-loopback interfaces that are up. A namespace whose `lo` bring-up silently failed (e.g. the ACK path in `bring_up_loopback` never being called by the unimplemented spawn loop) passes P4(a) and licenses `container`.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/probe.rs:600-625 (`decide_p4` link arm)_
- **[minor]** On normal exit `run_restore` returns `RestoreOutcome::Ran` without killing or reaping the restore phase's process group. The phase is deliberately given its own process group (`process_group(0)`) so the deadline can kill the group, but a restore script that backgrounds a downloader and exits leaves that process alive, holding the job's own network, into the runner phases that RF §7.1 says are loopback-only "without exception". RF §7.1 step 9 is "Reap every process group".
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:383 (`Ok(Some(_status)) => return RestoreOutcome::Ran`)_
- **[minor]** Two tests assert nothing an implementation could fail. `the_restore_script_is_addressed_in_trunk_and_never_in_a_checkout` follows an exact-equality assert on the constant with two derived asserts (`!starts_with('.')`, `starts_with("origin/")`) that are implied by it. `every_prerequisite_carries_the_words_the_diagnostic_prints` asserts only `!summary().is_empty()` and `(1..=5).contains(&number())`, the latter already pinned by the preceding test. Neither exercises a rule.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/m1.rs:759-765 and /Users/thettwe/Works/spine-kit/crates/spine-isolate/src/prereq.rs:481-488_
- **[minor]** `attempt_containment`'s absolute-path requirement is a `debug_assert!`, so it is compiled out of the release binary the collector actually ships. P1 is specified "By absolute path"; a relative path in release would silently measure the probe's cwd instead of the result directory.
  - _/Users/thettwe/Works/spine-kit/crates/spine-isolate/src/probe.rs:389_

### Spec divergences
- RF §7.1 P2, both limbs: the two identity installers each violate one. Arrangement 2 maps inside-100000 to outside-0 (host sees owner 0); arrangement 1 maps inside-0 to outside-100000 (probe reports uid 0). Both are excluded by P2 as written.
- RF §7.1 P3 first limb: the boundary provides no procfs, so the probe's process table is empty and the limb (plus the crate's own DERIVED empty-table guard) fails unconditionally. `mount_sequence` is presented as "RF §7.1's sequence, in its order" but is missing the step P3 depends on.
- RF §7.1 probe step 4 ("No probe artifact survives step 6, whatever the outcome") and §8.1 (one artifact, one path): a successful P1 limb (c) leaves a forged result file at the artifact path and nothing removes it.
- RF §7.1 P4(a) via the parser: a dump with no NLMSG_DONE is accepted as complete, so an interrupted enumeration is indistinguishable from a clean namespace — the very indistinguishability `decide_p4`'s Unavailable/empty arms were derived to close.
- RF §7.1 The deadline / §11 item 10: `run_restore` can block in `write_all` on the child's stdin before the deadline loop begins, so no deadline is enforced in that case.
- RF §7.1 M1 identity source arrangement 1 ("a range in /etc/subuid … the child runs as an id inside that range"): the delegated range's start is discovered nowhere and the mapper hard-codes 100000.
- RF §3 arrangement (b) for the result directory: the emitted mount plan neither hides the directory (the overlay's `/` lower layer exposes it) nor performs the move-into-place after reaping.
- RF §7.1 step 9 ("Reap every process group"): the restore phase's group is killed only on expiry, never on normal exit.
- DERIVED and correctly flagged by the builder, but worth restating as divergences a second implementation would not make: `MakeRootPrivate` prepended to the mount sequence; P1 limb (d) as `remove_dir_all` rather than `rmdir`; the P3 own-pid-present guard; the P4 Unavailable/zero-link guards; restore bytes on stdin; SIGKILL with no SIGTERM grace; the 10 ms try_wait poll; 100000 as the fixed unprivileged id; `setgroups=deny`; a delegated range of width 1; `size=4k` on tmpfs; `unshare(2)` as the creation mechanism; SYS_pivot_root 155/41; refusing `:`/`,` in an overlay lower path; reporting only the first absent prerequisite; treating an absent max_*_namespaces sysctl as no limit; `/etc/passwd` in place of NSS.
- Not implemented and therefore untested against the spec at all: the fork/clone-into-namespaces loop, the parent-writes-the-map synchronisation, the host-side stdout/stderr pipes (RF §7.1 "No runner stream is ever a file inside the boundary"), the probe's re-exec of the hash-verified binary (prerequisite 4), and P1(c)'s absolute-path construction. Every line under `#[cfg(target_os = "linux")]` — which is where all four of the every-landing findings live — has never been executed; `aarch64-unknown-linux-musl` was not even compile-checked.

## spine-resolve — verdict: needs-work

### Confirmed findings
- **[serious]** ts.rs `construct_end` runs PAST the end of an `export` declaration whose body is braced, so the `export` anchor steals the specifier of the NEXT import/re-export statement and resolves it WITHOUT that statement's type-only modifiers. Reproduced: `export enum E { A, B }\nimport type { X } from './a';` yields TWO sites — `Repo(["a.ts"])` at offset 0 and the correct `TypeOnly` at offset 23. Same for `export function f() {}\nexport type { T } from './a';` and `export interface I { a: 1 }\n… import type …`. IR §3.6 makes a type-only import contribute no edge and never freeze its target; IR §5.1 bounds the export clause at 'the next `;` or `}` at the same bracket depth'. The bug is line 255: `b'}' if depth > 0 => depth -= 1` swallows the declaration's own closing brace and keeps scanning into the following statement. Consequence: a `.d.ts`-adjacent type module enters the freeze closure, so `closure_digest` differs from a conforming implementation's and G8's CI recompute rejects the approval. Also violates IR §3.2's '(path, byte offset of the first token)' one-site-per-occurrence identity — a duplicate site double-counts §2.11's `unresolvable-import` when the swallowed specifier is unresolvable.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/ts.rs:245-261 (construct_end), used at ts.rs:352-355 (export_site) — vs import-resolver.md §5.1 and §3.6_
- **[serious]** yaml.rs `strip_comment` opens a comment at any `#` preceded by a space or tab, INCLUDING inside a single- or double-quoted scalar. Reproduced: `yaml::parse("name: \"a # b\"\n")` -> None, and `"description: 'uses # hash'\n"` -> None. The truncated line leaves an unbalanced quote, `unquote` refuses, and dart::rc returns `pubspec-not-declarative` — which under IR §3.8's language level makes EVERY Dart file in the repository contribute no edges and blocks every Dart landing there. IR §6.3 step 2 admits 'plain or single/double-quoted scalars only' and lists five excluded constructs; a `#` inside quotes is on neither list and is not a comment in YAML.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/yaml.rs:81-89 (strip_comment), yaml.rs:209-227 (unquote) — vs import-resolver.md §6.3 step 2, §6.7_
- **[serious]** dart.rs `union_of` collapses an ENTIRE conditional-import site to `unresolvable` when any single branch fails to resolve, discarding the branches that DID resolve. Reproduced: `import 'stub.dart' if (dart.library.io) 'io.dart';` with `lib/stub.dart` present and `io.dart` absent yields `Unresolvable(NoCandidate)` and NO edge to `lib/stub.dart`. IR §3.2 says 'a site may yield several `repo` targets … It is never a reason to call the site `unresolvable`', and §3.7's union rule says every branch contributes and all are resolved. The lost edge means a file the resolver could locate is not frozen; outside `H` that is only the `unresolvable-import-outside-harness` counter, so the miss is silent. Same shape applies to a branch whose target is a symlink/submodule, which drops the sibling edge too.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/dart.rs:222-252 (union_of, `other => return other` at line 248) — vs import-resolver.md §3.2, §3.7, case D4_
- **[serious]** Python compound one-liners draw NO import site at all. Reproduced: `try: import oracle\nexcept ImportError: pass` -> [], `if True: import oracle` -> [], `for x in y: import oracle` -> [], `with open(f): import oracle` -> []. (The multi-line `try:` form works.) The crate documents this as conforming to IR §4.1's anchor ('first token of a logical line or the first token after a `;`') and declines to widen it. But IR §3.7 names 'Python's `try: import … except ImportError:`' as a conditional construct whose every branch must contribute, and §3.4 rule 7 says 'a rule that ignored one would be a rule an oracle could hide behind'. This is the fail-OPEN reading of a genuine corpus ambiguity: an oracle imported behind `if True: import oracle` is never frozen, no edge is drawn, and nothing reports the miss — the exact silent-hole shape PB §6.7 / IR §7.3 removed Kotlin for.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/python.rs:118-141 (statement_sites, with the DEFECT note) — vs import-resolver.md §3.7 and §3.4 rule 7 against §4.1_
- **[minor]** lex.rs `scan_quoted` treats `\` as an escape inside a Dart RAW (`r'…'`) literal. Reproduced: `dart::sites("var s = r'a\\';\nimport 'x.dart';\n", …)` returns `[]` — the `\'` is consumed as an escape, the literal runs on past the newline, and the import on the following line vanishes entirely. Fail-open lexer bug; needs a raw literal ending in a backslash, which is rare but branch-controlled.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/lex.rs:236-256 (scan_quoted) called from lex.rs:550 (dart_string) — vs import-resolver.md §6.1_
- **[minor]** `ids::check_ambiguous_test_class` drops IR §11.5's 'under different targets' qualifier: it flags any repeated `(class-path, method)` pair. Reproduced: `check_ambiguous_test_class(["M.C/testX", "M.C/testX"])` -> `ambiguous-test-class: C/testX`, where IR §11.6 rule 2 makes a repeated identical id `duplicate-test-id` (a different finding). The r14 test only exercises the different-targets case, so the divergence is untested.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/ids.rs:257-274, test at ids.rs:553-567 — vs import-resolver.md §11.5, §11.6 rule 2_
- **[minor]** `pragma::IntentId` derives `Ord` structurally as (prefix, number, text), so `INT-999 < BUG-051` is true. intent-doc.md §3.1 states that G7's 'the lower intent id holds the lease' is a NUMERIC comparison, and templates.md §3.3 fixes one shared counter across `INT-`/`BUG-`, so the numeric comparison is well defined and the derived ordering contradicts it. The type's own doc-comment quotes G7. Nothing in this crate uses the ordering for a lease (only for canonical edge sorting), but it is a live trap for the consumer that will.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/pragma.rs:58-63 (derive(PartialOrd, Ord) on IntentId) — vs intent-doc.md §3.1, templates.md §3.3_
- **[minor]** `jsonc::scalar` accepts any run of alphanumerics/`-+._`, so `{"a": foo}` and `{"a": 01_2}` parse to `Json::Other` rather than failing. IR §5.3 step 1 fixes the dialect as 'JSON with comments and trailing commas (the dialect `tsc` accepts)'; tsc refuses bare identifiers as values, so a tsconfig that should be `tsconfig-unparseable` is silently read instead. Fail-open relative to the named dialect.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/jsonc.rs:226-234 — vs import-resolver.md §5.3 step 1_
- **[minor]** swift.rs finds the `targets:` label by scanning the WHOLE token stream for the first `targets` word followed by `:`, not the `Package(` initializer's own argument list. IR §7.3 rule 2 says 'Inside the initializer, the `targets:` argument is an array literal …'. Reproduced: a manifest whose `dependencies:` array carries a `targets:` label makes the wrong array the target list and yields `manifest-not-literal`.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/swift.rs:233-246 (parse_manifest) — vs import-resolver.md §7.3 rule 2_
- **[minor]** Three tests cannot fail. (a) `runner.rs::the_base_outcome_run_is_the_t_invocation_byte_for_byte` asserts `runner.base_outcome_run() == runner.invocation()`, but `base_outcome_run` IS `self.invocation()` (runner.rs:141) — it is `x == x` for every variant and pins nothing about IR §11.6 rule 4. (b) `ids.rs::fn_is_a_prefix_of_id_for_every_adapter_and_equal_for_three`'s `assert_eq!(f, id)` half is `id == id` by construction for the three non-pytest arms of `fn_of`. (c) `pragma.rs`'s `assert_eq!(joins_across_every_runner().len(), 4)` over a function whose body is `Runner::ALL`.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/runner.rs:137-142 and 306-315; ids.rs:110 and 431-445; pragma.rs:466-469 and 697_
- **[minor]** yaml.rs's merge-key (`<<`) refusal has no test that reaches it. The fixture listed for it in `every_construct_outside_the_declarative_subset_is_refused` — `"base: &b\n  x: 1\nc:\n  <<: *b\n"` — is refused by the `&b` anchor on line 1, before `block()` ever reaches the `<<` key. I confirmed separately that `"c:\n  <<: 1\n"` does hit the branch, so the code is right and only the test is vacuous for that row.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/yaml.rs:120-123 (the `<<` branch) and yaml.rs:266-276 (the test)_
- **[minor]** `dart::part_of` rebuilds the library-name index from scratch for every `part of <dotted-name>;` site — lexing every `.dart` blob in the whole tree, once per site, with no caching. That is O(sites x files x filesize) work a branch fully controls, in a crate whose glob module explicitly treats branch-controlled cost as 'a denial of service against every landing that reads its patterns'.
  - _/Users/thettwe/Works/spine-kit/crates/spine-resolve/src/dart.rs:281 (library_index call) and dart.rs:296-315 — vs the reasoning at glob.rs:269-274_

### Spec divergences
- Python: a relative import naming nothing is `external`, not `unresolvable`. IR §3.2 says '`unresolvable` is reserved for … a relative specifier that names nothing'; the crate follows §4.7's closed list ('arises in exactly these cases and no others'), which omits it. This is the fail-OPEN choice between two conflicting corpus sentences. Related and unnamed by either: `from .missing import x` in `pkg/m.py` yields `Repo(["pkg/__init__.py"])` — neither external nor unresolvable — because §4.3 adds the base package's `__init__.py` unconditionally.
- TypeScript: a non-simple literal specifier is reported `dynamic-import`. Case T20 requires only 'unresolvable' and §5.7's closed TS site list carries no `non-simple-literal` (Dart's §6.7 does). Documented in ts.rs `specifier_disposition`; the alternative would spend a token §5.7 does not name.
- TypeScript: `import './types'` where only `types.d.ts` exists is `no-candidate`, so IR §5.2's 'If every candidate is a declaration file, the site is `type_only`' — case T19 — is unreachable by its ordinary spelling, because step 3's extension list carries no declaration extension. The crate documents this as a spec defect and tests the residual explicitly. This is a defect in IR §5.2, not in the code.
- TypeScript: an `extends` whose relative target is absent from the tree, and an `extends` value that is neither string nor array, both yield `tsconfig-extends-external`. §5.3 step 2 fixes only three refusable forms and is silent on both. Derived, fail-closed, documented.
- TypeScript: `import {} from 's'` (empty named list) stays an ordinary value site. Not one of §3.6's closed forms. Derived.
- TypeScript: a `paths` substitution that lexically escapes the repository root contributes no candidate, and the alias may then `alias-dead-end`. §5.3 bounds `baseUrl` inside the repository and says nothing about a substitution. Derived, fail-closed.
- glob: the ORDER of `pattern-empty` / `pattern-too-long` / `pattern-illegal-byte` / `bad-negation` / `leading-slash` / `empty-segment` against each other is this implementation's. I read intent-doc.md §8.2 to check the claim: its normative order is over DOCUMENT parse steps, and 'within a step, the first failure in document line order wins' fixes nothing among one pattern's sub-statuses. The derived claim is correct. Only §6.2's 'brackets are validated over the whole pattern before it is split' is fixed, and the code honours it (`[a/b]` and `x/[a/b]/y` both -> bad-bracket).
- glob: `[a-]` reads a trailing `-` as a literal member ({a, -}); `[a-b-c]` reads members greedily left to right ({a..b, -, c}); `[**]` is `bad-globstar` because the `**`-in-a-segment scan is a literal byte scan that does not look outside brackets; `[^]` and `[]]` are legal. All derived from §6.2's grammar, all defensible, none contradicted by a published vector.
- Swift: IR §7.1 writes the extended-delimiter interpolation as `#\(`; Swift spells it `\#(`. The crate implements Swift's real spelling and reports §7.1's as wrong. Correct call — §7.1's own next sentence says no Swift import specifier is a string, so nothing turns on it.
- Swift: `mixed-objc-target` test 2 is evaluated over raw call tokens BEFORE rule 3's literal-subset validation, and `Target.unfollowable` is invented to give §7.8's `symlink-or-submodule` a site to be raised at (Swift resolves module names, not paths). Both derived and both fail-closed. A `Package.swift` with no `targets:` argument declares no targets and is legal rather than `manifest-not-literal` — also derived.
- Swift: within one manifest, `target-dir-missing` for an earlier target pre-empts `mixed-objc-target` for a later one, so the reason token is array-order dependent. Both are `lang-unclassifiable`, so the verdict is stable; only the `reason=` a human signs differs. §7.3 fixes only that `mixed-objc-target` precedes §3.3 Rule 2.
- lang: `lang(path)` reads §3.1's 'final component ends with' literally, so a file whose whole basename is `.py` is Python and `..dart` is Dart. No stem is required because no rule asks for one. Verified against the code.
- runner: `junit` and `kotest` are reserved. IR §11.1 files them 'contested' and §18 OPEN-12 asks the owner for one word; result-file.md §6.4 reserves both. The crate takes the union (`gradle`, `jest`, `junit`, `kotest`) and exposes `swift-testing` separately as reservation-recommended, matching §11.1's 'it is the owner's call, not this document's'. Consistent with OPEN-12's stated recommendation.
- pragma: §12.1's `<digit>+` is read as a maximal run with no trailing delimiter, so `@verifies INT-042/AC-1x` names AC-1; and the scan resumes one byte past a failed `@verifies` so a malformed occurrence cannot hide a later well-formed one. Both derived; both tested.
- jsonc: a duplicate key takes the LAST value (tsc's rule) rather than being refused as spine-canon's strict parser would. Derived, and correct — refusing would make an ordinary tsconfig `tsconfig-unparseable`.
- Builder report inaccuracy (not a code defect): the report claims 'intent-doc.md §9.5 matching vectors — all 33 rows'. The published table has 32 rows; the test array has 32 and I diffed it against the spec programmatically — exact match, same order. The count in the report is off by one.
